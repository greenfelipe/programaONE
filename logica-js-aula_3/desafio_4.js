let contador  = 0
let limite = prompt("Digite um número");

console.log("Iniciando contagem progressiva:");
while (contador < limite || contador == limite) {
    console.log(contador);
    contador++;
}
