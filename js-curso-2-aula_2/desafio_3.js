//Criar uma função que recebe um número como parâmetro e retorna o dobro desse número.

function exibeNumero(valor) {  
    return valor * 2;
}

let dobroNumero = exibeNumero(7);
console.log(dobroNumero);
